# functional-loginForm
users information is stored in a databse if user of same credentials already exists nothing is saved to the databse but if user of different credentials login it goes to the database. and all passwords are hashed
